package models.tower;

/**
 * Types of steps that can appear on a floor.
 */
public enum StepType {
    /** Standard combat encounter. */
    COMBAT,
    /** Reward encounter. */
    REWARD,
    /** Boss combat encounter. */
    BOSS
}
