package controllers.shared;

/**
 * Enumerate all the possible windows existing in the application
 */
public enum Windows {
    TEAM_SELECT,
    TEAM_SELECT_TOWER,
    TEAM_SELECT_EDITMODE,
    TEAM_MANAGEMENT,
    WIN,
    LOSE,
    HOMESCREEN,
    FLOOR_UNLOCK,
    TOWER_WIN,
}
